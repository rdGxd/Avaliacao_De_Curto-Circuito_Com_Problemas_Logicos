/*
JavaScript: && - Python: (and) -> Retornar o primeiro "valor falso", se existir uma expressão falsa nas expressões. Se todas as expressões forem verdadeiras, retorna o valor da última expressão.

JavaScript: || - Python: (or) -> Retornar o primeiro "valor verdadeiro", se existir uma expressão verdadeira nas expressões. Se todas as expressões forem falsas, retorna o valor da última expressão.
*/

/* OR
const corPadrao = "black";
let corUsuario = null || corPadrao;
*/

/* AND

const nome = "Rodrigo";
const sobrenome = "Silva";
const falaNome = () => {
  console.log(`${nome} ${sobrenome}`);
};

nome && sobrenome && falaNome();
 */
